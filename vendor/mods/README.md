# Vendor Mods

このフォルダは、ライセンス確認済みでプロジェクト内に置けるMOD jarを入れる場所です。

現在、標準マニフェストでこのフォルダから自動取り込みするファイルはありません。

将来 `mods_manifest.json` に `source: "bundled"` のMODを追加した場合、`MOD自動DL` を実行すると、ここにある対象jarをインスタンスの `mods` フォルダへコピーします。
